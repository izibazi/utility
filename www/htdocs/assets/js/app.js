Allblacks.readyBefore(this, function(lib) {
  var D = lib.D;
  D.enabled(false, false);
  var App = lib.app;
  var ImageButton = lib.ImageButton;
  ImageButton.interactImgs($('.on-off'), '', '_on');
});
